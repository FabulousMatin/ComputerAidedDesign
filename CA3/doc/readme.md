- to run and see the output just:
<do top_sim.tcl>

- the OFMs (output memories), and the input memory are in `./sim/file`.
- to see details, read the report placed in `./doc`.
- `./testcases` includes correct inputs and outputs.